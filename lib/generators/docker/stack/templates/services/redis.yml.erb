services:
  redis:
    image: redis:alpine
    ports:
      - "<%= 6380 + port_offset %>:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 3
