development:
  host: localhost
  port: Docker::Stack.port_for(:redis)
test:
  host: localhost
  port: Docker::Stack.port_for(:redis)
production:
  host: <%= ENV['REDIS_HOST'] || 'localhost' %>
  port: <%= ENV['REDIS_PORT'] || 6379 %>
